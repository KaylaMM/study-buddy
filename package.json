{
  "name": "study-buddy",
  "scripts": {
    "start": "cd server && npm start",
    "heroku-postbuild": "cd server && npm install",
    "build": "echo \"Skipping build\" && exit 0"
  },
  "engines": {
    "node": "18.x"
  }
}
